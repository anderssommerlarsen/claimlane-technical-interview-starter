# Client

Install and start

```
cd client
npm install
npm run dev
```

Client is hosted in localhost:5173

# Server

Install and start

```
cd server
npm install
npm run dev
```

Server is hosted on localhost:3000
